rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Each user can only read and write files under their own suppliers folder
    match /suppliers/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

  }
}
